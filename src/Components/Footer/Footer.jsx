import React from 'react'
import styles from './Footer.module.css';

const Footer = () => {

  const { app__footer } = styles;

  return (
    <footer className={app__footer}></footer>
  )
}

export default Footer